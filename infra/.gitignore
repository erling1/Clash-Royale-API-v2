# OpenTofu / Terraform — never commit state or secrets
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# Saved plan files (point-in-time artifacts from `tofu plan -out=...`)
*.plan
